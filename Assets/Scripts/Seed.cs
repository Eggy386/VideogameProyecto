using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Seed : MonoBehaviour
{
    //public delegate void PickedUp();
    //public event PickedUp OnPickedUp;

    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    if (collision.CompareTag("Player")) // Verifica si el jugador entra en contacto
    //    {
    //        Player player = collision.GetComponent<Player>();
    //        if (player != null)
    //        {
    //            // Crear un item correspondiente a la semilla recogida
    //            Item seedItem = GameManager.instance.itemManager.GetItemByName(this.name);

    //            // Añadir la semilla al inventario del jugador
    //            player.inventory.toolbar.AddItem(seedItem);

    //            Debug.Log($"Semilla {this.name} recogida.");
    //            Destroy(gameObject); // Destruir el objeto en el mundo
    //        }
    //    }
    //}
}
